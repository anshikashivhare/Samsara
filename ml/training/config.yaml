seed: 42
model:
  name: unet
  in_channels: 1
  out_channels: 1
training:
  epochs: 20
  batch_size: 8
  learning_rate: 0.0001
  image_size: 512
data:
  train_dir: data/processed/satellite/train
  validation_dir: data/processed/satellite/validation
