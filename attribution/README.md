# Attribution Engine

The attribution layer combines independent evidence rather than treating a single anomaly as proof of responsibility.

Current MVP score is an explainable weighted combination of spatial, temporal, trajectory, behaviour, and drift signals. XGBoost can be added later when labelled investigation outcomes exist.
